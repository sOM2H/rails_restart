require 'spec_helper'

describe RailsRestart do
  it 'has a version number' do
    expect(RailsRestart::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
